import pdf1 from '../../static/files/meeting-minutes/1. IEEE Executive Meeting Minutes (2022-07-10).pdf';

const pdfMap = {
    '1. IEEE Executive Meeting Minutes (2022-07-10).pdf': pdf1
};

const pdfs = Object.keys(pdfMap);

export { pdfs, pdfMap };
